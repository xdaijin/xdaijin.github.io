# Data Structure

## 数组、链表、跳表

## 栈、队列、优先队列、双端队列

## 哈希表、映射、集合

## 树、二叉树、二叉搜索树
树就是每个节点只有一个父节点，二叉树就是每个结点最多只有两个孩子结点，二叉搜索树就是结点之间有顺序关系，左结点小于父结点，右结点大于父结点

## 分治、回溯
回溯就是每次枚举加恢复现场

## 深度优先和广度优先
深度优先用stack，广度优先用queue

## 贪心算法
原理每次选择最优解

## 二分查找

## 动态规划
原理起点加递变公式，类似数学递增数列原理

## 并查集
merge：把两个连通分量合成一个连通分量 
find: 找到这个连通向量的根节点
rank: 用来给每个树加一个权值，合并的时候小权值成为大权值的子树，一定要用根节点的权值，子结点的权值不准

## 字典树和并查集
字典树就是每个结点下面有一个map构成的子结点列表

## 高级搜索

## AVL树和红黑树
### AVL
定义

#### 插入
* LL
* RR
* LR
* RL

#### 删除


## 位运算

## 布隆过滤器和LRU缓存

## 排序算法

## 字符串算法

## 匈牙利算法
原理能让位置就尽量让位置

## 哈夫曼树(最优二叉树)
原理就是把权重最小的值放在树的最深处